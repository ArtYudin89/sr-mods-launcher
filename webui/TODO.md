# Доработки лаунчера (заведено 2026-06-30)

## Статус 2026-06-30 (сделано, не собрано/не запушено)
Готово: **ВСЕ 1–16, включая 2.2**.
**#2.2 реализовано:** бейджи лагерей (UNI/REDUX/ORIG) в дереве и диалоге; диалог
«Добавить мод» = переключатель [🔎 По названию | 🗂 Лагерь→Пак→Мод | 🔗 По ссылке];
маркер ⚙ «патчит игру вне Mods» у паков (флаг `touches_game_root` в packs.json/
aggregate.py); пресеты «Всё <лагерь>» в окне сборок (компактно); правила: один вариант
на папку (`_dedup_folder`), зависимость наследует метку (`_pick_id_variant` + camp
через очередь resolve_set), конфликт label-agnostic (по базовому id). Резолвер
протестирован офлайн: dep `PolMercs` теперь резолвится (был missing), Utility берёт
redux-вариант при redux-родителе. proto_2_2.html — черновик-макет, можно удалить.
Плюс два бага сверх списка:
- **Детект Shu/Pol:** на известной базе детект обновлений теперь предпочитает вариант
  своего лагеря (`_best_unit_files(prefer_camp=...)`), поэтому genuine-Shu в redux-папке
  помечается «⬆ обновление» (раньше совпадал с Shu-вариантом → «актуально»).
- **Совместимость:** добавлена Name-aware проверка зависимостей подключённых в игре модов
  по диску (`_disk_dep_issues`) — ловит «PolMercsHQ требует PolMercs, а лежит ShuMercs».

Аудит каталога (689 модов): неразрешимых внутри-лагерных зависимостей всего 2 —
`OtherMods/DrKlesMod` (redux) требует `EvoArmada` (есть только в original/universe).
Каталог Shu/Pol-согласован; проблема пользователя была в состоянии диска, не в данных.

Артефакты агрегатора правились в ИСТОЧНИКЕ (mods.config.json) и синхронно в
state/packs.json + meta.json — НЕ забыть про reset+regen ботом, руками не мержить.
Нужно: пересобрать exe (build_rwt.bat / spec) и запушить агрегатор (Polaria-текст,
launcher_release.json).

---


1. **Сортировка по умолчанию.** «Как в игре» и «По разделам» сделать выбранными по умолчанию при первом запуске.
2. **Терминология/гибкость лагерей:**
   - 2.1. «redux» → «ПБ Свободная Бухта»; «universe» → «Space Rangers Universe (Community)».
   - 2.2. Разрешить ставить любые паки и моды независимо от лагеря, но для модов показывать отметку лагеря (бейдж UNI/REDUX, как на скриншотах вики).
3. **Иконка программы** — взять `C:\claude_sandbox\game_launcher\Icon.ico`, вшить в exe (обе сборки: public + RWT) и в окно pywebview.
4. **Две кнопки «Обновить» путают.** Переименовать одну (или обе) — развести по смыслу (обновить каталог vs обновить установленный мод).
5. **Polaria** — убрать приписку «общая для лагерей».
6. **Убрать лишние упоминания redux** там, где не нужно (напр. подсказка к полю при создании сборки) — чтобы не создавалось впечатление приоритета одного лагеря.
7. **Свёрнутое состояние** — показывать счётчик модов по статусам (цветные цифры через «/»).
8. **Очистка папки модов** — второе подтверждение, кнопку согласия разместить в другом месте (избежать случайного двойного клика).
9. **Базовые моды из комплекта игры не удалять.** Список (из инсталлятора, скриншот): `ModCFG.txt`, и в `Mods\Tweaks\`: `German`, `Spanish`, `LeoDomikShipsUpdate15`, `LeoDomikShipsUpdate30`, `SR2LoadingScreen`, `SR2PQuestStyle`. При очистке `Mods\*` эти сохранять (инсталлятор кладёт их в `TempStorageFolder` и возвращает обратно).
10. **Управление профилями** — вместо «...» кнопка «+». После добавления профиля не закрывать окно, остаться в списке для выбора.
11. **Поиск** — искать также по кратким описаниям.
12. **Полное описание** — строки вида `FullDescription=<clr><clrEnd>` понимать как пропуск (пустые).
13. **Регрессия:** пропали цветовые теги из полного описания — вернуть.
14. **Самообновление лаунчера** — проверять наличие новой версии самого лаунчера.

15. **Время в логе** — для каждого сообщения в логе писать таймстамп (напр. `[ЧЧ:ММ:СС]`).

16. **Хранилище данных лаунчера** — рабочие данные (напр. `launcher_config.json`, профили, лог) хранить в `%AppData%` (или ином подходящем месте), а не рядом с exe.

## Заметки по реализации
- Фронт: `web/index.html`, `web/style.css`, `web/app.js`. Бэкенд js_api: `app.py`. Движок: `launcher_core`.
- Бейджи лагерей (п.2.2) и описания модов берутся из ModuleInfo → `catalog.json` (см. агрегатор).
- Иконка (п.3) — прописана `icon='Icon.ico'` в обоих .spec; файл `webui/Icon.ico`.
- **СБОРКА (репо публичный, RWT НЕ используется):** `cd webui && pyinstaller --noconfirm --clean SRModsLauncher.spec` при ЗАКРЫТОМ exe (иначе WinError 5). Корневой `build.bat` = ДРУГОЙ старый tkinter — не путать. RWT-спека осталась в репо, но она deprecated.
- **Версия:** `LAUNCHER_VERSION` в app.py = 0.14.0 (плейсхолдер — выставить реальный след. номер, текущий публичный > 0.13.1). `state/launcher_release.json` в агрегаторе — источник для самопроверки.
